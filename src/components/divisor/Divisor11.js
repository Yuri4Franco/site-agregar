import React from "react";
import "./Divisor11.css"; // Vamos separar o CSS

const Divisor11 = () => {
    return (
        <div className="divisor-container">
            <hr className="linha-divisor" />
            <div className="triangulo-divisor" />
        </div>
    );
};

export default Divisor11;
